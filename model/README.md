# Saved model folder

Run `python train_model.py` once after installing the requirements. It creates
`milkguard_model.joblib` in this folder. The binary model is intentionally not
hand-edited: it is reproduced from the documented synthetic data generator and
fixed random seed in `train_model.py`.
