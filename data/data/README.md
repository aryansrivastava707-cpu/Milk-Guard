# Data notes

`demo_samples.csv` contains five manual test entries for the website.

`synthetic_milk_data.csv` is generated by `python train_model.py`. It is not a
real food-testing dataset. See the root README for the assumptions and how to
replace it with measured, independently labelled data.
